 #include "main.h"
 /**
  * _islower - islower function
  * @c: c is the paramter
  * Return: 1 on success, 0 on failure
  * Descriptioin: read descirip
  */
int _islower(int c)
{
if (c >= 97 && c <= 122)
return (1);
else
return (0);
}
