 #include "main.h"
 /**
  * islower - islower function
  *
  * Return: 0 on success, 1 on failure
  *
  * Description: find lower or not
 */
int _islower(int c)
{
if (c >= 97; && c <=122;)
return (1);
else
return (0);
}
